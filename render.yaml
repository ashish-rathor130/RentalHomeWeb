services:
  - type: web
    name: rentalhomeweb
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libjpeg-dev zlib1g-dev
      pip install -r requirements.txt
    startCommand: gunicorn rental_home.wsgi:application
